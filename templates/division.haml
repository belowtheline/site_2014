.row
  .span12
    .page-header
      %h1= division['name']
  .span6
    .lead.text-center House of Representatives
    %p
      Your local member of the House of Representatives is 
      - if representative['party'] then
        <strong>#{representative['first_name']} #{representative['last_name']}</strong> of the <span class="party party-#{representative['party']}">#{parties[representative['party']]['name']}</span>.
      - else
        <strong>#{representative['first_name']} #{representative['last_name']}</strong>, an independent.
    %hr
      Currently known candidates for the this division are:
      %table.table.table-condensed
        - candidates.each do |candidate|
          %tr
            %td
              #{candidate['first_name']} #{candidate['last_name']}
            %td
              = partytmpl.render(Object.new, :code => candidate['party'], :parties => parties)
  .span6
    .lead.text-center Senate
    %p
      The Senators for your #{state_or_territory} are listed below. To see more
      information about Senate representation for your #{state_or_territory}
      click <a href="/#{division['state']}.html">here</a>.
    %table.table.table-condensed
      - senators.each do |senator|
        %tr
          %td
            #{senator['first_name']} #{senator['last_name']}
          %td
            = partytmpl.render(Object.new, :code => senator['party'], :parties => parties)
