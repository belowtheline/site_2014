.row
  .span12
    .page-header
      %h1= division['name']
  .span6
    .lead.text-center House of Representatives
    %p
      Your local member of the House of Representatives is 
      - if representative['party'] then
        #{representative['first_name']} #{representative['last_name']} of the <span class="party party-#{representative['party']}">#{parties[representative['party']]['name']}</span>.
      - else
        #{representative['first_name']} #{representative['last_name']}, an independent.
    %hr
      Currently known candidates for the division of #{division['name']} are:
      %table.table.table-condensed
        - candidates.each do |candidate|
          %tr
            %td
              #{candidate['first_name']} #{candidate['last_name']}
            %td
              %span(class="partycode party party-#{candidate['party'] or 'ind'}")
                #{parties.has_key? candidate['party'] and parties[candidate['party']]['code'] or 'IND'}
  .span6
    .lead.text-center Senate
    %p
      The Senators for your #{state_or_territory} are:
    %table.table.table-condensed
      - senators.each do |senator|
        %tr
          %td
            #{senator['first_name']} #{senator['last_name']}
          %td
            %span(class="partycode party party-#{senator['party'] or 'ind'}")
              #{parties.has_key? senator['party'] and parties[senator['party']]['code'] or 'IND'}
#push
